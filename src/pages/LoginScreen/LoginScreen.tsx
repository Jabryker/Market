import { FC } from "react";
import { LoginPageTemplate } from "../../templates/";

export const LoginScreen: FC = () => {
  return (
    <div>
      <LoginPageTemplate />
    </div>
  );
};
